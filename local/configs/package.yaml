---
name: svgen
version: "{{major}}.{{minor}}.{{patch}}"
description: "A tool for working with scalable vector graphics."
entry: {{entry}}
requirements: []
dev_requirements:
  - setuptools-wrapper
  - vcorelib
